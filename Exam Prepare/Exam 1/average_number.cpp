


#include <iostream>
using namespace std;

int main()
{
    int input;
    int threeDigits = 0;
    int fourDigits = 0, fiveDigits = 0, sixDigits = 0, sevenDigits = 0, MoreOddDigits = 0, equalDigits = 0;

    cout << "Please enter a sequence of numbers in between 1 and 100, " << endl;
    cout << "each one in a separate line.End your sequence by typing -1 : " << endl;

    // get each line number;

         // jump out of the loop;



        // get each digit in the input number;


        // get input number;



        // get the remain number;

        //even odd


        //digit count



    cout << "Total count of numbers in the 3-digits Numbers group:" << threeDigits << endl;
    cout << "Total count of numbers in the 4-digits Numbers group:" << fourDigits << endl;
    cout << "Total count of numbers in the 5-digits Numbers group:" << fiveDigits << endl;
    cout << "Total count of numbers in the 6-digits Numbers group:" << sixDigits << endl;
    cout << "Total count of numbers in the 7-digits Numbers group:" << sevenDigits << endl;
    cout << "Total count of numbers in the More Odd-digits group:" << MoreOddDigits << endl;
    cout << "Total count of numbers in the Equal-digits group:" << equalDigits << endl;

    return 0;
}